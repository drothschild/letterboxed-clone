import React, { Component } from 'react';
import Auth from './Auth';
export default class Main extends Component {
    render() {
        return (
            <Auth>
                <h1>Welcome to Letterboxd(clone)</h1>
            </Auth>
        );
    }
}
